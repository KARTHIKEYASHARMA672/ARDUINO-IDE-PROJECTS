CONNECTIONS:-
| Component    | Pin      | Arduino Uno Pin | Notes                  |
| ------------ | -------- | --------------- | ---------------------- |
| DHT22        | 1 (VCC)  | 5V              | —                      |
| DHT22        | 2 (DATA) | D2              | 10kΩ pull-up           |
| DHT22        | 4 (GND)  | GND             | —                      |
| Relay Module | VCC      | 5V              | —                      |
| Relay Module | GND      | GND             | —                      |
| Relay Module | IN       | D7              | —                      |
| Load Device  | NO/COM   | Device wiring   | For AC/DC load control |


CODE:-
#include "DHT.h"

#define DHTPIN 2
#define DHTTYPE DHT22
#define RELAY 7

DHT dht(DHTPIN, DHTTYPE);

void setup() {
  pinMode(RELAY, OUTPUT);
  dht.begin();
}

void loop() {
  float t = dht.readTemperature();

  if (!isnan(t)) {
    if (t > 28) { 
      digitalWrite(RELAY, HIGH); // Turn ON fan
    } else {
      digitalWrite(RELAY, LOW);  // Turn OFF fan
    }
  }
  delay(2000);
}
