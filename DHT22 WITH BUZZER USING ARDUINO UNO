CONNECTIONS:-
| Component | Pin      | Arduino Uno Pin | Notes        |
| --------- | -------- | --------------- | ------------ |
| DHT22     | 1 (VCC)  | 5V              | —            |
| DHT22     | 2 (DATA) | D2              | 10kΩ pull-up |
| DHT22     | 4 (GND)  | GND             | —            |
| Buzzer +  | +        | D8              | —            |
| Buzzer –  | -        | GND             | —            |


CODE:-

#include "DHT.h"

#define DHTPIN 2
#define DHTTYPE DHT22
#define BUZZER 8

DHT dht(DHTPIN, DHTTYPE);

void setup() {
  pinMode(BUZZER, OUTPUT);
  dht.begin();
}

void loop() {
  float t = dht.readTemperature();

  if (!isnan(t)) {
    if (t > 30) {  // Alert if above 30°C
      digitalWrite(BUZZER, HIGH);
    } else {
      digitalWrite(BUZZER, LOW);
    }
  }
  delay(2000);
}
